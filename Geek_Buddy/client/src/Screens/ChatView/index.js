import React, { Component } from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ChatScreen = () => {
  return (
    <SafeAreaView>
      <Text> Chat ....InComponent </Text>
    </SafeAreaView>
  );
};
export default ChatScreen;
